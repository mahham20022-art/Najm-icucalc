name: Deploy SeizureMeter to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'seizuremeter/**'
      - '.github/workflows/deploy-seizuremeter.yml'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        working-directory: seizuremeter
        run: flutter pub get

      - name: Analyze
        working-directory: seizuremeter
        run: flutter analyze

      - name: Build web (release)
        working-directory: seizuremeter
        run: flutter build web --release --base-href /Najm-icucalc/seizuremeter/

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: seizuremeter/build/web
          destination_dir: seizuremeter
          keep_files: true
          commit_message: 'Deploy SeizureMeter: ${{ github.event.head_commit.message }}'
